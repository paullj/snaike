{
  "name": "snaike",
  "version": "1.0.0",
  "description": "An snake AI using NEAT",
  "private": true,
  "repository": "https://github.com/paullj/snake-ai.git",
  "author": "Paul Lavender-Jones <paul@lavender-jones.com>",
  "license": "MIT",
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "compile": "tsc --build --force tsconfig.json",
    "export": "yarn compile && yarn workspace @snaike/web build",
    "clean": "rm -rf packages/neat/dist"
  },
  "devDependencies": {
    "typescript": "^4.2.4"
  }
}
